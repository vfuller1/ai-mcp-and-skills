[project]
name = "mcp-client"
version = "0.1.0"
description = "Basic MCP Client using OpenAI tool calling"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]
