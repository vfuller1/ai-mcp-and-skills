[project]
name = "weather"
version = "0.1.0"
description = "Basic MCP Weather Server with get_alerts and get_forecast tools"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.0.0",
    "httpx>=0.27.0",
]
